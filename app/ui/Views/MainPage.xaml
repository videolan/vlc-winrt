<Page
    x:Class="VLC_WINRT.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mainPage="using:VLC_WINRT.Views.Controls.MainPage"
    mc:Ignorable="d"
    d:DesignHeight="768"
    DataContext="{StaticResource MainViewModel}">

    <Grid Background="#FF1D1D1D">
        <Grid Margin="30,20,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                       x:Uid="MainPageTitle"
                       Text="VLC for WinRT"
                       Style="{StaticResource TitleTextStyle}"
                       Foreground="White"
                       FontSize="26"
                       Margin="0,10">
            </TextBlock>
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Grid.Row="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="550"></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Grid Grid.Column="0" DataContext="{Binding LastViewedVM}" Style="{StaticResource ColumnGridStyle}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" x:Uid="LastViewed" Text="last viewed"
                                   Style="{StaticResource ColumnHeaderTextStyle}">
                        </TextBlock>
                        <Button Margin="5,0" Style="{StaticResource TextButtonStyle}" Grid.Row="0" Grid.Column="1"
                                HorizontalAlignment="Right" Content="Clear">
                        </Button>

                        <mainPage:LastViewedThumbnail Grid.Row="1"
                                                      Grid.ColumnSpan="2"
                                                      DataContext="{Binding LastViewedVM}"
                                                      >
                        </mainPage:LastViewedThumbnail>

                        <mainPage:LastViewedThumbnail Grid.Row="2"
                                                      Grid.Column="0"
                                                      DataContext="{Binding SecondLastViewedVM}"
                                                      >
                        </mainPage:LastViewedThumbnail>

                        <mainPage:LastViewedThumbnail Grid.Row="2"
                                                      Grid.Column="1"
                                                      DataContext="{Binding ThirdLastViewedVM}">
                        </mainPage:LastViewedThumbnail>
                    </Grid>


                    <StackPanel Grid.Column="1" Style="{StaticResource ColumnStackPanelStyle}"
                                DataContext="{Binding VideoVM}">
                        <TextBlock x:Uid="Movies" Text="movies" Style="{StaticResource ColumnHeaderTextStyle}"></TextBlock>
                        <ItemsControl ItemsSource="{Binding Media}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid HorizontalAlignment="Left" Height="100" Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"></ColumnDefinition>
                                            <ColumnDefinition Width="300"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0"
                                                Background="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}"
                                                Width="100">
                                            <Image Source="{Binding Image}" Stretch="UniformToFill"
                                                   AutomationProperties.Name="{Binding Title}" />
                                        </Border>
                                        <StackPanel Grid.Column="1"
                                                    Background="{StaticResource ListViewItemOverlayBackgroundThemeBrush}">
                                            <TextBlock Text="{Binding Title}"
                                                       Foreground="{StaticResource ListViewItemOverlayForegroundThemeBrush}"
                                                       Style="{StaticResource TitleTextStyle}" Height="60"
                                                       Margin="15,0,15,0" />
                                            <TextBlock Text="{Binding Subtitle}"
                                                       Foreground="{StaticResource ListViewItemOverlaySecondaryForegroundThemeBrush}"
                                                       Style="{StaticResource CaptionTextStyle}" TextWrapping="NoWrap"
                                                       Margin="15,0,15,10" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Style="{StaticResource ColumnStackPanelStyle}"
                                DataContext="{Binding MusicVM}">
                        <TextBlock x:Uid="Music" Text="music" Style="{StaticResource ColumnHeaderTextStyle}"></TextBlock>
                        <ItemsControl ItemsSource="{Binding Media}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid HorizontalAlignment="Left" Height="100" Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"></ColumnDefinition>
                                            <ColumnDefinition Width="300"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0"
                                                Background="{StaticResource ListViewItemPlaceholderBackgroundThemeBrush}"
                                                Width="100">
                                            <Image Source="{Binding Image}" Stretch="UniformToFill"
                                                   AutomationProperties.Name="{Binding Title}" />
                                        </Border>
                                        <StackPanel Grid.Column="1"
                                                    Background="{StaticResource ListViewItemOverlayBackgroundThemeBrush}">
                                            <TextBlock Text="{Binding Title}"
                                                       Foreground="{StaticResource ListViewItemOverlayForegroundThemeBrush}"
                                                       Style="{StaticResource TitleTextStyle}" Height="60"
                                                       Margin="15,0,15,0" />
                                            <TextBlock Text="{Binding Subtitle}"
                                                       Foreground="{StaticResource ListViewItemOverlaySecondaryForegroundThemeBrush}"
                                                       Style="{StaticResource CaptionTextStyle}" TextWrapping="NoWrap"
                                                       Margin="15,0,15,10" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
    <Page.BottomAppBar>
        <AppBar Padding="10,0,10,0">
            <Grid>
                <Button Style="{StaticResource VideoAppBarButtonStyle}" 
                        Command="{Binding PickVideo}"
                        HorizontalAlignment="Left"></Button>
                <Button Style="{StaticResource CalculatorAppBarButtonStyle}"
                        Click="Calculator_Click"
                        HorizontalAlignment="Right"></Button>
            </Grid>
        </AppBar>
    </Page.BottomAppBar>
</Page>