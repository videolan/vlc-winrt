<Page
    x:Class="VLC_WINRT.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VLC_WINRT.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#FF1D1D1D">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" 
                   Text="VLC for Windows 8" 
                   Style="{StaticResource GroupHeaderTextStyle}" 
                   HorizontalAlignment="Center" 
                   Margin="50"></TextBlock>
        <ScrollViewer Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="last viewed" Style="{StaticResource ColumnHeaderTextStyle}"></TextBlock>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="music" Style="{StaticResource ColumnHeaderTextStyle}"></TextBlock>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="movies" Style="{StaticResource ColumnHeaderTextStyle}"></TextBlock>
                </StackPanel>
            </Grid>
         
            
            
        </ScrollViewer>
      
    </Grid>
</Page>