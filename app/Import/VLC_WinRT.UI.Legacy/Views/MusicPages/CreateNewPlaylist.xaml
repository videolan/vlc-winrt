<Page x:Class="VLC_WinRT.Views.MusicPages.CreateNewPlaylist"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:VLC_WinRT.Views.MusicPages"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:userControls="using:VLC_WinRT.Views.UserControls"
             mc:Ignorable="d"
             d:DesignWidth="346">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="{StaticResource FrameMarginGridLength}" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="{StaticResource FrameMarginGridLength}" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="{StaticResource FrameMarginGridLength}" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="{StaticResource FrameMarginGridLength}" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="1"
                   Margin="{StaticResource FrameMargin}"
                   TextTrimming="CharacterEllipsis"
                   TextWrapping="NoWrap"
                   VerticalAlignment="Center"
                   Text="{Binding Source={StaticResource Strings}, Path=NewPlaylist}"
                   Style="{StaticResource SubheaderTextBlockStyle}" />
        <userControls:FocusTextBox Grid.Row="5"
                                   x:Name="playlistName"
                                   Margin="{StaticResource FrameMargin}"
                                   PlaceholderText="{Binding Source={StaticResource Strings}, Path=PlaylistNamePlaceholder}" />
        <AppBarButton Grid.Row="7"
                      Style="{StaticResource AppBarButtonStyleHorizontal}"
                      Margin="{StaticResource FrameMargin}"
                      Label="{Binding Source={StaticResource Strings}, Path=AddToCollection}"
                      Click="AddToCollection_Click" />
    </Grid>
</Page>
