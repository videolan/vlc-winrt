namespace VLC_WinRT.Model
{
    public enum OrderType
    {
        ByArtist = 0,
        ByDate = 1,
        ByAlbum = 2,
    }

    public enum OrderListing
    {
        Ascending = 0,
        Descending = 1,
    }
}
