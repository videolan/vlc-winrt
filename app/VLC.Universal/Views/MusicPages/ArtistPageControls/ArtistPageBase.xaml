<Page x:Class="VLC.UI.Views.MusicPages.ArtistPageControls.ArtistPageBase"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:VLC.UI.Views.MusicPages.ArtistPageControls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:triggers="using:WinRT.Triggers"
      mc:Ignorable="d"
      d:DesignHeight="300"
      d:DesignWidth="400"
      DataContext="{Binding Source={StaticResource Locator}, Path=MusicLibraryVM}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <local:MainArtistHeader x:Name="MainArtistHeader"
                                VerticalAlignment="Stretch" />


        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
            <Button Content="{Binding Source={StaticResource Strings}, Path=Albums}"
                    Command="{Binding CurrentArtist.NavigateArtistInfoView}"
                    Style="{StaticResource TextBlockButtonStyle}"
                    Margin="{StaticResource FrameMarginBothSides}" />
            <Button Content="{Binding Source={StaticResource Strings}, Path=MoreInfo}"
                    Command="{Binding CurrentArtist.NavigateArtistInfoView}"
                    Style="{StaticResource TextBlockButtonStyle}"
                    Margin="{StaticResource FrameMarginBothSides}" />
        </StackPanel>
        <local:ArtistAlbumsSemanticView x:Name="ArtistAlbumsSemanticView"
                                        Grid.Row="2"/>

        <local:ArtistInfoControl x:Name="ExtraInfoGrid"
                                 x:DeferLoadStrategy="Lazy"
                                 Grid.Row="2"
                                 Visibility="Collapsed" />

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Tiny">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="ArtistAlbumsSemanticView.FloatingActionButtonVisibility"
                                Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Medium">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="650" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="ArtistAlbumsSemanticView.FloatingActionButtonVisibility"
                                Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
