<Page x:Class="VLC.UI.UWP.Views.VariousPages.ExternalStorageIncludePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:VLC.UI.UWP.Views.VariousPages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:toolkitControls="using:Microsoft.Toolkit.Uwp.UI.Controls"
      mc:Ignorable="d">
    <StackPanel Background="{ThemeResource ApplicationOverlayTranslucentThemeColor}">
        <toolkitControls:HeaderedTextBlock
            Header="{Binding Source={StaticResource Strings}, Path=ExternalStorageDeviceDetected}"
            Text="{Binding Source={StaticResource Strings}, Path=WhatToDo}" />

        <StackPanel>
            <RadioButton x:Name="Index" Content="{Binding Source={StaticResource Strings}, Path=IndexExternalStorage}" IsChecked="True" />
            <RadioButton x:Name="Select" Content="{Binding Source={StaticResource Strings}, Path=SelectContentToCopy}" />
            <RadioButton x:Name="DoNothing" Content="{Binding Source={StaticResource Strings}, Path=DoNothing}" />
        </StackPanel>

        <CheckBox x:Name="Remember" Content="{Binding Source={StaticResource Strings}, Path=RememberChoice}" IsChecked="True" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1"
                    Click="Button_Click">Ok</Button>
        </Grid>
    </StackPanel>
</Page>