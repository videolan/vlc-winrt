 <!--x:Name="MenuFlyout"-->
    
    <MenuFlyout 
            x:Class="VLC.Universal8._1.Views.UserControls.Flyouts.FileExplorerItemFlyout"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:local="using:VLC.Universal8._1.Views.UserControls.Flyouts"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">
    <MenuFlyoutItem Text="{Binding Source={StaticResource Strings}, Path=CopyToLocalStorage}"
                    Command="{Binding Source={StaticResource Locator}, Path=FileExplorerVM.CurrentStorageVM.CopyCommand}"
                    CommandParameter="{Binding}"
                    Style="{StaticResource MenuFlyoutItemIconTemplate}"
                    Tag="{StaticResource AddSymbol}"/>
</MenuFlyout>
