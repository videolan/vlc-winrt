namespace VLC_WINRT_APP.Model
{
    public enum OrderType
    {
        ByArtist = 0,
        ByDate = 1,
    }

    public enum OrderListing
    {
        Ascending = 0,
        Descending = 1,
    }
}
