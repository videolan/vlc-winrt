<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:VLC_WINRT_APP.Resources">

    <Flyout x:Key="PhoneOpenStreamFlyout"
            Placement="Full">
        <Grid>
            <StackPanel VerticalAlignment="Center"
                        Background="WhiteSmoke">
                <TextBlock Text="Open a stream"
                           Style="{StaticResource SubheaderTextBlockStyle}"
                           Margin="12" />
                <StackPanel Orientation="Horizontal"
                            Margin="12">
                    <TextBox Name="UrlTextBox"
                             PlaceholderText="Enter a URL"
                             Width="350" />
                    <Button Style="{StaticResource TextBlockButtonStyle}"
                            Margin="12,0,0,0"
                            Command="{Binding Source={StaticResource Locator}, Path=VideoLibraryVM.PlayNetworkMRL}"
                            CommandParameter="{Binding ElementName=UrlTextBox, Path=Text}">
                        <Button.Content>
                            <SymbolIcon Symbol="Accept" />
                        </Button.Content>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Flyout>

    <MenuFlyout x:Key="PhoneChangeMusicViewFlyout"
                Placement="Top">
        <MenuFlyoutItem Text="albums"
                        Command="{Binding Source={StaticResource Locator}, Path=MainVM.ChangeMainPageMusicViewCommand}"
                        CommandParameter="0"/>
        <MenuFlyoutItem Text="artists"
                        Command="{Binding Source={StaticResource Locator}, Path=MainVM.ChangeMainPageMusicViewCommand}"
                        CommandParameter="1" />
        <MenuFlyoutItem Text="songs"
                        Command="{Binding Source={StaticResource Locator}, Path=MainVM.ChangeMainPageMusicViewCommand}"
                        CommandParameter="2" />
        <MenuFlyoutItem Text="playlists"
                        Command="{Binding Source={StaticResource Locator}, Path=MainVM.ChangeMainPageMusicViewCommand}"
                        CommandParameter="3" />
    </MenuFlyout>
    
    <MenuFlyout x:Key="PhoneChangeVideoViewFlyout"
                Placement="Top">
        <MenuFlyoutItem Text="all videos"
                        Command="{Binding Source={StaticResource Locator}, Path=MainVM.ChangeMainPageVideoViewCommand}"
                        CommandParameter="0" />
        <MenuFlyoutItem Text="shows"
                        Command="{Binding Source={StaticResource Locator}, Path=MainVM.ChangeMainPageVideoViewCommand}"
                        CommandParameter="1" />
        <MenuFlyoutItem Text="camera roll"
                        Command="{Binding Source={StaticResource Locator}, Path=MainVM.ChangeMainPageVideoViewCommand}"
                        CommandParameter="2" />
    </MenuFlyout>
</ResourceDictionary>
