<UserControl x:Class="VLC_WINRT_APP.Views.MainPages.MusicPanes.ArtistCollectionPanes.ArtistsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:VLC_WINRT_APP.Views.MainPages.MusicPanes.ArtistCollectionPanes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:triggers="using:WinRT.Triggers"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0"
                   Grid.Row="0"
                   Style="{StaticResource LittleHeaderTextBlockStyle}"
                   TextWrapping="NoWrap"
                   TextTrimming="CharacterEllipsis">
            <Run Text="{Binding Artists.Count}" />
            <Run Text="ARTISTS" />
        </TextBlock>
        <ListView x:Name="ArtistListView"
                  Grid.Row="1"
                  Grid.Column="0"
                  ItemsSource="{Binding Artists}"
                  IsItemClickEnabled="True"
                  SelectionMode="None"
                  ItemContainerStyle="{StaticResource ListViewItemStyle}"
                  ItemTemplate="{StaticResource ArtistHorizontalTemplate}"
                  ItemClick="ArtistListView_OnItemClick">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal"
                                   ItemWidth="300" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>
</UserControl>
