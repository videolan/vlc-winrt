<UserControl x:Class="VLC_WINRT_APP.Views.MainPages.MusicPanes.MusicPanesControls.MusicPaneComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:VLC_WINRT_APP.Views.MusicPages"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">

    <ComboBox x:Name="MusicViewComboBox"
              DataContext="{Binding Source={StaticResource Locator}, Path=SettingsVM}"
              ItemsSource="{Binding MusicViewCollection}"
              SelectedItem="{Binding MusicView, Mode=TwoWay}"
              BorderThickness="1"
              BorderBrush="{StaticResource MainColor}"
              Style="{StaticResource HeaderComboBoxStyle}"
              Background="Transparent"
              DropDownOpened="ComboBox_OnDropDownOpened"
              DropDownClosed="ComboBox_OnDropDownClosed">
        <ComboBox.ItemTemplate>
            <DataTemplate>
                <TextBlock Margin="0,0,6,0"
                           Text="{Binding Converter={StaticResource MusicViewStringifyConverter}}" />
            </DataTemplate>
        </ComboBox.ItemTemplate>
    </ComboBox>
</UserControl>
