<UserControl x:Class="VLC_WINRT_APP.Views.UserControls.Flyouts.OpenStreamFlyout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:VLC_WINRT_APP.Views.UserControls.Flyouts"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">

    <Border Background="WhiteSmoke"
            BorderBrush="DimGray"
            BorderThickness="1">
        <Grid VerticalAlignment="Center"
              Background="WhiteSmoke">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock x:Uid="OpenStream"
                       Text="Open a stream"
                       Style="{StaticResource SubheaderTextBlockStyle}"
                       Margin="12" />
            <Grid Grid.Row="1"
                  Margin="12,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="50" />
                </Grid.ColumnDefinitions>
                <TextBox x:Uid="EnterAUrl"
                         Name="UrlTextBox"
                         PlaceholderText="Enter a URL"
                         InputScope="Url" />
                <Button Grid.Column="1"
                        Command="{Binding Source={StaticResource Locator}, Path=VideoLibraryVM.PlayNetworkMRL}"
                        CommandParameter="{Binding ElementName=UrlTextBox, Path=Text}"
                        Style="{StaticResource TextBlockButtonStyle}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch">
                    <Button.Content>
                        <SymbolIcon Symbol="Accept" />
                    </Button.Content>
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>
