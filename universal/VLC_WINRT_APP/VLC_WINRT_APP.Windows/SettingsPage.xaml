<Page
    x:Class="VLC_WINRT.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VLC_WINRT.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:VLC_WINRT.Views.Controls"
    xmlns:userControls="using:VLC_WINRT_APP.Views.UserControls"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator}, Path=SettingsVM}">

    <Page.Resources>
        <DataTemplate x:Key="StorageFolderDataTemplate">
            <TextBlock Margin="5,0,0,0">
                <Run Text="{Binding DisplayName}" />
                <LineBreak />
                <Run Text="{Binding Mru}"/>
            </TextBlock>
        </DataTemplate>
    </Page.Resources>
    <Grid x:Name="RootGrid" Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="5,40,0,0" x:Name="HeaderGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Style="{StaticResource PreviousAppBarButtonStyle}" Click="GoBack_Click" />
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <TextBlock x:Uid="MainPageTitle" Text="VLC for Windows 8" Margin="3,0,0,0" FontWeight="SemiBold" FontSize="20"/>
                <TextBlock Grid.Row="1" Text="settings" Style="{StaticResource SubheaderTextBlockStyle}"/>
            </Grid>
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource SearchNoTextAppBarButtonStyle}" Click="OpenSearchPane" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" x:Name="SectionsGrid" RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <CompositeTransform/>
            </Grid.RenderTransform>
            <ScrollViewer x:Name="MainScrollViewer"
                HorizontalScrollBarVisibility="Auto"
                          HorizontalScrollMode="Auto"
                          VerticalScrollBarVisibility="Disabled"
                          VerticalScrollMode="Disabled"
                          ZoomMode="Disabled">
                <StackPanel Orientation="Horizontal" Margin="0,70,0,50">
                    <Grid Margin="50,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock  FontSize="42" Style="{StaticResource VLCSubHeaderStyle}">
                            <Run Text="♪" />
                            <Run x:Uid="CustomMusicFolder" Foreground="WhiteSmoke"/>
                        </TextBlock>
                        <Grid Grid.Row="1" Margin="-17,0,0,0">
                            <Button Style="{StaticResource AddLittleAppBarButtonStyle}" Command="{Binding AddCustomAudioFolder}"/>
                        </Grid>
                        <Grid Grid.Row="2" Margin="-10,0,0,0">
                            <ListView ItemContainerStyle="{StaticResource ListViewItemContainerStyle}" ItemsSource="{Binding MusicFolders}" ItemTemplate="{StaticResource StorageFolderDataTemplate}" SelectionChanged="MusicListView_SelectionChanged"/>
                        </Grid>
                    </Grid>
                    <Grid Margin="90,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock FontSize="42" Style="{StaticResource VLCSubHeaderStyle}">
                            <Run Text="" FontSize="30" FontFamily="Segoe UI Symbol"/>
                            <Run x:Uid="CustomVideoFolder" Foreground="WhiteSmoke"/>
                        </TextBlock>
                        <Grid Grid.Row="1" Margin="-17,0,0,0">
                            <Button Style="{StaticResource AddLittleAppBarButtonStyle}" Command="{Binding AddCustomVideoFolder}"/>
                        </Grid>
                        <Grid Grid.Row="2">
                            <ListView ItemContainerStyle="{StaticResource ListViewItemContainerStyle}" ItemsSource="{Binding VideoFolders}" ItemTemplate="{StaticResource StorageFolderDataTemplate}" SelectionChanged="MovieListView_SelectionChanged"/>
                        </Grid>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
        <userControls:MiniPlayer Grid.Row="2"/>
    </Grid>
</Page>
