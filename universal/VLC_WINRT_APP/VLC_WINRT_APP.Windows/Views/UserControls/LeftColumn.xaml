<UserControl x:Class="VLC_WINRT_APP.Views.UserControls.LeftColumn"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:VLC_WINRT_APP.Views.UserControls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:triggers="using:WinRT.Triggers"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">
    <Grid x:Name="ColumnGrid"
          Width="340"
          Background="#19FFFFFF">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="1" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="42" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="24" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="24" />
            <RowDefinition Height="*" />
            <RowDefinition Height="24" x:Name="SeparatorRowDefinition"/>
            <RowDefinition Height="315" x:Name="MiniPlayersRowDefinition"/>
        </Grid.RowDefinitions>
        <Grid Background="#4CFFFFFF"
              Grid.Column="1"
              Grid.RowSpan="8" />
        <StackPanel Orientation="Horizontal"
                    Height="45"
                    Margin="42,0,0,0"
                    x:Name="HeaderGrid"
                    Grid.Row="1">
            <Path Data="{StaticResource VLCFlatPath}"
                  Stretch="Uniform"
                  Fill="#FFFFFFFF"
                  Width="30"
                  Height="30" />
            <TextBlock Text="{StaticResource AppTitleW8}"
                       x:Name="TitleTextBlock"
                       Style="{StaticResource TitleTextBlockStyle}"
                       FontSize="18"
                       Margin="6,-2,0,0"
                       Foreground="WhiteSmoke"
                       VerticalAlignment="Center" />
        </StackPanel>
        <SearchBox Grid.Row="3"
                   x:Name="LargeSearchBox"
                   Height="37"
                   FontSize="17"
                   Margin="42,0,20,0"
                   PlaceholderText="Search"
                   SuggestionsRequested="LargeSearchBox_SuggestionsRequested"
                   QuerySubmitted="LargeSearchBox_QuerySubmitted"
                   ResultSuggestionChosen="LargeSearchBox_OnResultSuggestionChosen" />
        <Button Grid.Row="3"
                x:Name="LittleSearchBox"
                Content=""
                FontFamily="Segoe UI Symbol"
                FontSize="18"
                HorizontalAlignment="Center"
                Style="{StaticResource TextBlockButtonStyle}"
                Visibility="Collapsed"/>
        <ListView Grid.Row="5"
                  x:Name="PanelsListView"
                  ItemsSource="{Binding Panels}"
                  IsItemClickEnabled="True"
                  SelectionMode="None"
                  ItemContainerStyle="{StaticResource ListViewItemContainerStyle}"
                  ItemTemplate="{StaticResource SidebarItemTemplate}"
                  ScrollViewer.VerticalScrollBarVisibility="Hidden"
                  ScrollViewer.VerticalScrollMode="Enabled">
            <triggers:Interactions.Triggers>
                <triggers:EventTrigger EventName="ItemClick">
                    <triggers:InvokeCommandAction Command="{Binding GoToPanelCommand}"
                                                  PassEventArgsToCommand="True" />
                </triggers:EventTrigger>
            </triggers:Interactions.Triggers>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Width="340" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
        <Grid Background="#4CFFFFFF"
              Grid.ColumnSpan="2"
              Grid.Row="6"
              Height="1"
              VerticalAlignment="Center"
              Margin="42,0" />
        <local:MiniPlayer Grid.Row="7"
                          VerticalAlignment="Bottom" />
        <local:LoadingMusicLibrary Grid.Row="7"
                                   VerticalAlignment="Bottom" />
        <local:MiniVideoPlayer Grid.Row="7"
                               VerticalAlignment="Bottom" />
    </Grid>
</UserControl>