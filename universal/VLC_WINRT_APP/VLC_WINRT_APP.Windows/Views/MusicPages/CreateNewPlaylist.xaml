<SettingsFlyout x:Class="VLC_WINRT_APP.Views.MusicPages.CreateNewPlaylist"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:local="using:VLC_WINRT_APP.Views.MusicPages"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:userControls="using:VLC_WINRT_APP.Views.UserControls"
                mc:Ignorable="d"
                IconSource="Assets/SmallLogo.png"
                Title="Create new playlist"
                d:DesignWidth="346"
                Style="{StaticResource SettingsFlyoutStyle}">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <userControls:HeaderControl HeaderTextBlock="New playlist" />
        <StackPanel Margin="24">
            <TextBlock Name="body"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       TextWrapping="Wrap">
                <TextBlock.Text>
                    The playlist will also be available on your Windows computer, laptop or tablet, only if the audio files are on your other devices.
                </TextBlock.Text>
            </TextBlock>
            <userControls:FocusTextBox x:Name="playlistName"
                     Margin="0,12,0,0"
                     PlaceholderText="Playlist name" />
            <Button Margin="0,6"
                    Content="Add to collection"
                    Click="AddToCollection_Click" />
        </StackPanel>
    </StackPanel>
</SettingsFlyout>
