<UserControl
    x:Class="VLC_WINRT_APP.Views.MusicPages.MiniPlayer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VLC_WINRT_APP.Views.MusicPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
             d:DesignWidth="400"
             DataContext="{Binding Source={StaticResource Locator}, Path=MusicPlayer}"
             Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}">

    <Grid Background="#FF1E1E1E"
          Height="70">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="70" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="70" />
            <ColumnDefinition Width="12" />
        </Grid.ColumnDefinitions>
        <Image Source="{Binding CurrentPlayingArtist.CurrentAlbumItem.Picture}"
               Grid.Column="1"
               Stretch="UniformToFill"
               Height="70"
               Width="70"
               Tapped="UIElement_OnTapped" />
        <StackPanel Grid.Column="2"
                    VerticalAlignment="Center"
                    Margin="12,0,0,0"
                    Tapped="UIElement_OnTapped">
            <TextBlock Text="{Binding CurrentPlayingArtist.CurrentAlbumItem.CurrentTrack.Name}"
                       FontSize="26"
                       FontWeight="Normal" />
            <TextBlock Text="{Binding CurrentPlayingArtist.Name}"
                       FontSize="18"
                       FontWeight="Normal"
                       Foreground="#FF838383"
                       Typography.Capitals="SmallCaps" />
        </StackPanel>
        <AppBarToggleButton Grid.Column="3"
                            Label="Like"
                            IsChecked="{Binding CurrentPlayingArtist.CurrentAlbumItem.CurrentTrack.Favorite}"
                            Command="{Binding CurrentPlayingArtist.CurrentAlbumItem.CurrentTrack.FavoriteTrack}"
                            CommandParameter="{Binding CurrentPlayingArtist.CurrentAlbumItem.CurrentTrack}">
            <AppBarToggleButton.Icon>
                <FontIcon Glyph="&#xE0A5;" />
            </AppBarToggleButton.Icon>
        </AppBarToggleButton>
    </Grid>
</UserControl>
