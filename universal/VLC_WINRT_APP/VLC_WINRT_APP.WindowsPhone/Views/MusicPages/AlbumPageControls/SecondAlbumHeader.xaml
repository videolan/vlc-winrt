<UserControl x:Class="VLC_WINRT_APP.Views.MusicPages.AlbumPageControls.SecondAlbumHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:VLC_WINRT_APP.Views.MusicPages.AlbumPageControls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:triggers="using:WinRT.Triggers"
             xmlns:placeholder="using:VLC_WINRT_APP.Views.UserControls.Placeholder"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">

    <Grid Margin="0,0,12,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Margin="24,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Add to playlist"
                       Style="{StaticResource SubheaderTextBlockStyle}" />
            <AppBarButton Grid.Column="1"
                          Label="new playlist"
                          Icon="Add"
                          Style="{StaticResource AppBarButtonStyleHorizontal}"
                          Command="{Binding ShowCreateNewPlaylistPaneCommand}" />
        </Grid>
        <!-- /!\ Playlists not implemented yet /!\ -->
        <ListView Grid.Row="1"
                  ItemsSource="{Binding TrackCollections}"
                  SelectedItem="{Binding CurrentTrackCollection, Mode=TwoWay}"
                  Margin="24,0,0,0"
                  ItemTemplate="{StaticResource PhoneTrackCollectionTemplate}"
                  ItemContainerStyle="{StaticResource HorizontalListViewItemStyle}">
            <triggers:Interactions.Triggers>
                <triggers:EventTrigger EventName="SelectionChanged">
                    <triggers:InvokeCommandAction Command="{Binding AddToPlaylistCommand}"
                                                  CommandParameter="{Binding CurrentAlbum}" />
                </triggers:EventTrigger>
            </triggers:Interactions.Triggers>
        </ListView>
        <placeholder:NoPlaylistPlaceholder Grid.Row="1"
                                           Visibility="{Binding TrackCollections.Count, Converter={StaticResource CountToVisibilityConverter}}"
                                           Margin="24,0,0,0" />
        <Grid Background="Transparent"
              Grid.Row="0"
              Grid.RowSpan="2"
              Tapped="SwypeLeftToRight_Button_Tap">
            <Button Content=""
                    FontFamily="Segoe UI Symbol"
                    FontSize="32"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Margin="6,0,0,0"
                    Style="{StaticResource TextBlockButtonStyle}"
                    Click="SwypeLeftToRight_Button_Click" />
        </Grid>
    </Grid>
</UserControl>
