<UserControl x:Class="VLC_WINRT_APP.Views.UserControls.VLCSearchBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:VLC_WINRT_APP.Views.UserControls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">

    <UserControl.Resources>
    </UserControl.Resources>

    <Grid DataContext="{Binding Source={StaticResource Locator}, Path=MainVM}">
        <AutoSuggestBox IsTabStop="False"
                        x:Uid="SearchField"
                        PlaceholderText="Search"
                        Text="{Binding SearchTag, Mode=TwoWay}"
                        Margin="0,0,20,0"
                        IsEnabled="{Binding Source={StaticResource Locator}, Path=VideoVm.IsPlaying, Converter={StaticResource VLCSearchBoxConverter}}" >
            <AutoSuggestBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Text}" />
                </DataTemplate>
            </AutoSuggestBox.ItemTemplate>
        </AutoSuggestBox>
    </Grid>
</UserControl>
