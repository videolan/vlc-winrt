<UserControl x:Class="VLC_WINRT_APP.Views.MainPages.MainLastViewedPinnedPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:VLC_WINRT_APP.Views.MainPages"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:triggers="using:WinRT.Triggers"
             xmlns:controls="using:VLC_WINRT_APP.Controls"
             mc:Ignorable="d">

    <!--<Grid>
        <ScrollViewer Style="{StaticResource VerticalScrollViewerStyle}">
            <StackPanel Orientation="Horizontal">
                <GridView DataContext="{Binding Source={StaticResource Locator}, Path=MusicLibraryVM}"
                          ItemsSource="{Binding FavoriteAlbums}"
                          Header="Favorite Albums"
                          HeaderTemplate="{StaticResource ListViewHeaderTemplate}"
                          ItemTemplate="{StaticResource SemanticZoomInViewAlbumTemplate}">
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VariableSizedWrapGrid Orientation="Horizontal"
                                                   ItemHeight="140"
                                                   ItemWidth="140" />
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                </GridView>
            </StackPanel>
        </ScrollViewer>
    </Grid>-->

    <Grid>
        <Hub x:Name="MainHub"
             Orientation="Vertical">
            <Hub.Header>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource TextBlockButtonStyle}"
                            FontSize="22"
                            FontWeight="Light"
                            Command="{Binding Source={StaticResource Locator}, Path=VideoLibraryVM.PickVideo}">
                        <Button.Content>
                            <TextBlock>
                                <Run Text="open a video" />
                                <Run Text=""
                                     FontFamily="Segoe UI Symbol" />
                            </TextBlock>
                        </Button.Content>
                    </Button>
                    <Button Margin="24,0,0,0"
                            Style="{StaticResource TextBlockButtonStyle}"
                            FontSize="22"
                            FontWeight="Light"
                            Command="{Binding Source={StaticResource Locator}, Path=VideoLibraryVM.PickVideo}">
                        <Button.Content>
                            <TextBlock>
                                <Run Text="open a stream" />
                                <Run Text=""
                                     FontFamily="Segoe UI Symbol" />
                            </TextBlock>
                        </Button.Content>
                    </Button>
                </StackPanel>
            </Hub.Header>
            <Hub.Sections>
                <HubSection>
                    <HubSection.ContentTemplate>
                        <DataTemplate>
                            <Grid Margin="0,24,0,0"
                                  DataContext="{Binding Source={StaticResource Locator}, Path=MusicLibraryVM}">
                                <GridView ItemsSource="{Binding FavoriteAlbums}"
                                          Header="Favorite Albums"
                                          HeaderTemplate="{StaticResource ListViewHeaderTemplate}"
                                          ItemTemplate="{StaticResource SemanticZoomInViewAlbumTemplate}">
                                    <GridView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <VariableSizedWrapGrid Orientation="Horizontal"
                                                                   ItemHeight="200"
                                                                   ItemWidth="200" />
                                        </ItemsPanelTemplate>
                                    </GridView.ItemsPanel>
                                </GridView>
                                <controls:NoFavoriteAlbumsControl DataContext="{Binding FavoriteAlbums}"/>
                            </Grid>
                        </DataTemplate>
                    </HubSection.ContentTemplate>
                </HubSection>
                <HubSection DataContext="{Binding Source={StaticResource Locator}, Path=VideoLibraryVM}">
                    <HubSection.ContentTemplate>
                        <DataTemplate>
                            <GridView ItemsSource="{Binding ViewedVideos}"
                                      ItemTemplate="{StaticResource SemanticZoomInViewMovieTemplate}"
                                      IsItemClickEnabled="True"
                                      SelectionMode="None"
                                      Header="Last Viewed"
                                      HeaderTemplate="{StaticResource ListViewHeaderTemplate}">
                                <triggers:Interactions.Triggers>
                                    <triggers:EventTrigger EventName="ItemClick">
                                        <triggers:InvokeCommandAction Command="{Binding Source={StaticResource Locator}, Path=VideoLibraryVM.OpenVideo}"
                                                                      PassEventArgsToCommand="True" />
                                    </triggers:EventTrigger>
                                </triggers:Interactions.Triggers>
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                            </GridView>
                        </DataTemplate>
                    </HubSection.ContentTemplate>
                </HubSection>
            </Hub.Sections>
        </Hub>
    </Grid>
</UserControl>
