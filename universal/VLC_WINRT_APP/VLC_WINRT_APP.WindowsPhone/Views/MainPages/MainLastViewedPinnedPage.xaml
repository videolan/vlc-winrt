<UserControl
    x:Class="VLC_WINRT_APP.Views.MainPages.MainLastViewedPinnedPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VLC_WINRT_APP.Views.MainPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer Style="{StaticResource VerticalScrollViewerStyle}">
            <StackPanel Orientation="Horizontal">
                <GridView DataContext="{Binding Source={StaticResource Locator}, Path=MusicLibraryVM}"
                          ItemsSource="{Binding FavoriteAlbums}"
                          Header="Favorite Albums"
                          HeaderTemplate="{StaticResource ListViewHeaderTemplate}"
                          ItemTemplate="{StaticResource SemanticZoomInViewAlbumTemplate}">
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VariableSizedWrapGrid Orientation="Horizontal"
                                                   ItemHeight="140"
                                                   ItemWidth="140" />
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                </GridView>
            </StackPanel>
        </ScrollViewer>
    </Grid>

</UserControl>
