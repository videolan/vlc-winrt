<Page x:Class="VLC_WINRT_APP.Views.MainPages.MainPageHome"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:VLC_WINRT_APP.Views.MainPages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:musicPages="using:VLC_WINRT_APP.Views.MusicPages"
      xmlns:triggers="using:WinRT.Triggers"
      xmlns:controls="using:VLC_WINRT_APP.Controls"
      xmlns:userControls="using:VLC_WINRT_APP.Views.UserControls"
      mc:Ignorable="d"
      DataContext="{Binding Source={StaticResource Locator}, Path=MainVM}">

    <Page.Resources>
    </Page.Resources>

    <Grid Margin="0"
          x:Name="MainGrid"
          DataContext="{Binding Source={StaticResource Locator}, Path=MusicLibraryVM}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <ScrollViewer>
            <StackPanel Margin="12,0,0,0">
                <TextBlock Text="Favorites and suggestions"
                           Margin="0,36,0,0"
                           FontSize="32"
                           Foreground="{StaticResource MainColor}"
                           Style="{StaticResource HeaderTextBlockStyle}" />
                <StackPanel x:Name="MusicLibrary"
                            DataContext="{Binding Source={StaticResource Locator}, Path=MusicLibraryVM}">
                    <TextBlock Text="albums"
                               Margin="0,12,0,0"
                               FontSize="24"
                               Style="{StaticResource SubheaderTextBlockStyle}" />
                    <GridView ItemsSource="{Binding RandomAlbums}"
                              ItemTemplate="{StaticResource SemanticZoomInViewAlbumTemplateLittleSize}"
                              IsItemClickEnabled="True"
                              SelectionMode="None"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Disabled"
                              ScrollViewer.HorizontalScrollMode="Enabled"
                              ScrollViewer.VerticalScrollMode="Disabled">
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                    </GridView>
                    <controls:NoFavoriteAlbumsControl HorizontalAlignment="Left" />
                </StackPanel>
                <StackPanel x:Name="VideoLibrary"
                            DataContext="{Binding Source={StaticResource Locator}, Path=VideoLibraryVM}">
                    <TextBlock Text="videos"
                               Margin="0,12,0,0"
                               FontSize="24"
                               Style="{StaticResource SubheaderTextBlockStyle}" />
                    <ListView ItemsSource="{Binding Videos}"
                              ItemTemplate="{StaticResource VideoTemplateWP}"
                              Margin="-12,0,0,0"
                              ScrollViewer.VerticalScrollBarVisibility="Disabled">
                        <ListView.ItemContainerTransitions>
                            <TransitionCollection>
                                <EntranceThemeTransition />
                                <RepositionThemeTransition />
                            </TransitionCollection>
                        </ListView.ItemContainerTransitions>
                    </ListView>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar Style="{StaticResource VLCCommandBarStyle}">
            <AppBarButton Icon="OpenFile"
                          Label="open a file" />
            <AppBarButton Label="open a stream"
                          Icon="World" />
            <CommandBar.SecondaryCommands>

                <AppBarButton Label="special thanks" />
                <AppBarButton Label="settings" />
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
