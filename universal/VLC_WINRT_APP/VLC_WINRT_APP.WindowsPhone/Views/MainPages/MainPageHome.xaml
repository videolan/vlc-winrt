<Page x:Class="VLC_WINRT_APP.Views.MainPages.MainPageHome"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:VLC_WINRT_APP.Views.MainPages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:musicPages="using:VLC_WINRT_APP.Views.MusicPages"
      mc:Ignorable="d"
      DataContext="{Binding Source={StaticResource Locator}, Path=MainVM}">

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Image Source="/Assets/background.jpg"
               Stretch="UniformToFill"
               Opacity="0.4"
               Grid.RowSpan="4" />
        <StackPanel Orientation="Horizontal"
                    Margin="16,20,0,0">
            <Path Data="{StaticResource VLCFlatPath}"
                  Stretch="Uniform"
                  Fill="#FFFFFFFF"
                  Width="32"
                  Height="32" />
            <TextBlock Text="{StaticResource AppTitle}"
                       Style="{StaticResource TitleTextBlockStyle}"
                       Typography.Capitals="SmallCaps"
                       FontSize="23"
                       Margin="12,0,0,0"
                       VerticalAlignment="Center" />
        </StackPanel>
        <Hub x:Name="MainHub"
             x:FieldModifier="public"
             Grid.Row="2">
            <Hub.Sections>
                <HubSection Header="HOME">
                    <HubSection.ContentTemplate>
                        <DataTemplate>
                            <local:MainLastViewedPinnedPage Margin="-16,0,0,0"/>
                        </DataTemplate>
                    </HubSection.ContentTemplate>
                </HubSection>
                <HubSection Header="VIDEOS">
                    <HubSection.ContentTemplate>
                        <DataTemplate>
                            <local:MainPageVideos />
                        </DataTemplate>
                    </HubSection.ContentTemplate>
                </HubSection>
                <HubSection Header="MUSIC">
                    <HubSection.ContentTemplate>
                        <DataTemplate>
                            <local:MainPageMusic />
                        </DataTemplate>
                    </HubSection.ContentTemplate>
                </HubSection>
            </Hub.Sections>
        </Hub>
        <musicPages:MiniPlayer Grid.Row="3" />
    </Grid>
    <Page.BottomAppBar>
        <CommandBar ClosedDisplayMode="Minimal">
            
        </CommandBar>
    </Page.BottomAppBar>
</Page>
