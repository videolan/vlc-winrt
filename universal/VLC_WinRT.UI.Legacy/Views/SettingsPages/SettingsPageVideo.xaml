<UserControl x:Class="VLC_WinRT.UI.Legacy.Views.SettingsPages.SettingsPageVideo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:VLC_WinRT.UI.Legacy.Views.SettingsPages"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="1000"
             d:DesignWidth="400">

    <Grid x:Name="RootGrid"
          Background="{ThemeResource ApplicationOverlayTranslucentThemeBrush}"
          DataContext="{Binding Source={StaticResource Locator}, Path=SettingsVM}">
        <ScrollViewer Style="{StaticResource VerticalScrollViewerStyle}">
            <StackPanel x:Name="RootPanel"
                        Margin="{StaticResource FrameMarginBothSidesWithTitleMargin}"
                        MaxWidth="550">
                <StackPanel Orientation="Horizontal">
                    <AppBarButton Style="{StaticResource AppBarButtonStyleHorizontal}"
                                  HorizontalAlignment="Right"
                                  Command="{Binding ChangeSettingsViewCommand}"
                                  CommandParameter="-1"
                                  Width="50">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="{StaticResource PreviousPageSymbol}"
                                      Margin="0,3,0,0"
                                      FontFamily="ms-appx:VLC.ttf#W10" />
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <TextBlock Text="Video" x:Uid="VideosHeader"
                               Style="{StaticResource SubheaderTextBlockStyle}" />
                </StackPanel>
                <TextBlock Text="Video playback" x:Uid="VideoPlayback"
                           FontSize="22"
                           Style="{StaticResource SubheaderTextBlockStyle}"
                           FontWeight="SemiLight" />
                <ToggleSwitch x:Uid="VideoPlaybackExplain" Header="Hardware decoding when possible (experimental)"
                              IsOn="{Binding HardwareAccelerationEnabled, Mode=TwoWay}"
                              Margin="-6,0,0,0" />
                <TextBlock x:Uid="VideoPlaybackExplainTwo" Text="Hardware decoding may not work on your device. You need to restart the app to apply changes"
                           Style="{StaticResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource PivotHeaderForegroundUnselectedBrush}"
                           Margin="0,-6,0,12" />
                
                <TextBlock x:Uid="SubtitlesEncoding" Text="Subtitles encoding"
                           Margin="0,12,0,0"
                           FontSize="22"
                           Style="{StaticResource SubheaderTextBlockStyle}"
                           FontWeight="SemiLight" />
                <ComboBox Margin="0,12"
                          ItemsSource="{Binding SubtitlesEncodingValues}"
                          SelectedItem="{Binding SubtitleEncodingValue, Mode=TwoWay}"
                          Style="{StaticResource VLCLightComboBox}"
                          Background="{ThemeResource ApplicationBackgroundQuarterTranslucentThemeBrush}"
                          Width="160">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Tag="WindowsOnly"
                           x:Uid="VideoFolders"
                           Text="Video folders"
                           FontSize="22"
                           Style="{StaticResource SubheaderTextBlockStyle}"
                           FontWeight="SemiLight" />
                <TextBlock Tag="WindowsOnly"
                           x:Uid="VideoSettings"
                           Text="VLC will only search videos that are in your video library. You can add folders to your library here:"
                           Style="{StaticResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource PivotHeaderForegroundUnselectedBrush}" />
                <ListView Tag="WindowsOnly"
                          ItemsSource="{Binding VideoFolders}"
                          ItemContainerStyle="{StaticResource ListViewItemStyle}"
                          SelectionMode="None"
                          IsItemClickEnabled="False">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Background="Transparent">
                                <Grid.Resources>
                                    <Flyout x:Key="RemoveFlyout">
                                        <Border RequestedTheme="Dark"
                                                Background="{ThemeResource ApplicationOverlayTranslucentThemeBrush}"
                                                BorderBrush="DimGray"
                                                BorderThickness="1"
                                                Padding="12">
                                            <Grid Width="250">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="12" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                <TextBlock x:Uid="RemoveFolderExplain" Text="If you don't want this folder in your Video library, you can remove it. It will affect all apps, not only VLC."
                                                           Style="{StaticResource BodyTextBlockStyle}" />
                                                <Button Grid.Row="2"
                                                        Content="Remove this folder"
                                                        x:Uid="RemoveFolder"
                                                        Command="{Binding Source={StaticResource Locator}, Path=SettingsVM.RemoveFolderFromVideoLibrary}"
                                                        CommandParameter="{Binding}" />
                                            </Grid>
                                        </Border>
                                    </Flyout>
                                </Grid.Resources>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="{StaticResource FrameMarginGridLength}" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <FontIcon Glyph="{StaticResource FolderSymbol}"
                                          FontFamily="ms-appx:VLC.ttf#W10"
                                          Foreground="{StaticResource SemiLightMainColor}" />
                                <TextBlock Grid.Column="2"
                                           TextWrapping="Wrap"
                                           FlyoutBase.AttachedFlyout="{StaticResource RemoveFlyout}"
                                           Tapped="VideoFolder_Tapped"
                                           VerticalAlignment="Center">
                                    <Run Text="{Binding Path}"
                                         FontSize="14"
                                         Foreground="{ThemeResource PivotHeaderForegroundUnselectedBrush}" />
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Button Tag="WindowsOnly"
                        Content="Add a new folder"
                        x:Uid="AddFolder"
                        Command="{Binding AddFolderToLibrary}"
                        CommandParameter="{Binding VideoLibraryId}" />

                <TextBlock Tag="WindowsOnly"
                           Text="Video Library"
                           x:Uid="VideoLibrary"
                           FontSize="22"
                           Margin="0,20,0,0"
                           Style="{StaticResource SubheaderTextBlockStyle}"
                           FontWeight="SemiLight" />
                <ToggleSwitch Tag="WindowsOnly"
                              x:Uid="VideoPlaybackBackground"
                              Header="Video playback in background"
                              IsOn="{Binding ContinueVideoPlaybackInBackground, Mode=TwoWay}"
                              Margin="-6,0,0,0" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
