<UserControl
    x:Class="VLC_WinRT.Views.UserControls.Placeholder.StartSearchingSomething"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VLC_WinRT.Views.UserControls.Placeholder"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
  <Grid>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock Text=""
                   FontFamily="Segoe UI Symbol"
                   Foreground="{StaticResource SemiLightMainColor}"
                   FontSize="80"
               HorizontalAlignment="Center"
               TextAlignment="Center"
               VerticalAlignment="Center" />
    <TextBlock Grid.Row="1"
               Style="{StaticResource SubheaderTextBlockStyle}"
               VerticalAlignment="Center"
               TextAlignment="Center">
        <Run Text="Start searching something!"
             FontSize="20" />
    </TextBlock>
  </Grid>
</UserControl>
